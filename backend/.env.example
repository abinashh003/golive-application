PORT=5000
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=livestream
DB_PORT=5432
JWT_SECRET=change-this-to-a-long-random-string
CORS_ORIGIN=http://localhost:5173

# Optional: only needed when running multiple backend replicas (e.g. in
# Kubernetes). Leave unset for local single-instance development.
REDIS_HOST=
REDIS_PORT=6379
